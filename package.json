{
  "name": "evoting-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd evoting-frontend && npm run build",
    "start": "node server/server.js"
  },
  "dependencies": {
    "react-scripts": "^5.0.1"
  }
}
